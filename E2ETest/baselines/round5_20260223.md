# E2E Baseline Round 5 (2026-02-23)

## Run Scope

- Project A: `e2e_dispatch_chain_v1`
- Project B: `e2e_team_discuss_v1`
- Mode: backend API only, concurrent run

## Artifact Locations

- Chain artifacts: `D:\AgentWorkSpace\TestTeam\TestRound20\docs\e2e\20260223_195623-precheck`
- Discuss artifacts: `D:\AgentWorkSpace\TestTeam\TestTeamDiscuss\docs\e2e\20260223_200024`

## Final Snapshot

- `e2e_dispatch_chain_v1`: all execution tasks closed, all sessions idle.
- `e2e_team_discuss_v1`: all execution tasks closed, all sessions idle.

## Analyzer Result (Current Rule Set)

### Chain (`analyze-core-logs.ps1`)

- `overall_pass: False`
- Pass:
  - seeded tasks/dependency structure
  - dependency gate precheck for `B1` and `C`
  - eventual dispatch for `B1` and `C`
  - no open execution tasks
  - no running sessions
  - no `list_directory` noise
- Fail:
  - `ORCHESTRATOR_DISPATCH_FAILED = 1`
  - `team_tool_success_rate = 0.6742` (< 0.80)

### Discuss (`analyze-discuss-logs.ps1`)

- `overall_pass: False`
- Pass:
  - seeded tasks/dependency structure
  - invalid parent dependency create rejected (`409`)
  - dependency gate precheck for `B/C/D`
  - discuss flow exists
  - no open execution tasks
  - no running sessions
  - no dispatch failures
- Fail:
  - "Three draft tasks were eventually dispatched" check (`B=False`)
  - "Alignment task was dispatched" check (`False`)

## Notes

- Functional closure happened in both projects (task closure + idle sessions), but analyzer policy still flags this round as fail due to strict dispatch-path expectations and tool success threshold.
- Use this baseline to track:
  - dispatch failure count trend,
  - team tool success-rate trend,
  - discuss scenario dispatch-path consistency.
